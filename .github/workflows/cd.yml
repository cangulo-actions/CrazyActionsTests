name: CD
on:
  push:
    branches:
      - main

jobs:
  semver:
    name: release new version
    runs-on: ubuntu-latest
    steps:
      - name: semver
        uses: cangulo-actions/semver/release@main
        with:
          create-gh-release: false