name: CD
on:
  push:
    branches:
      - main

jobs:
  semver:
    name: release new version
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: semver
        uses: cangulo-actions/semver@init
        id: semver
        with:
          create-gh-release: true
          print-summary: true
      - name: print semver output
        run: |
          echo "version: ${{ steps.semver.outputs.version }}"
          echo "release-title: ${{ steps.semver.outputs.release-title }}"
          echo "release-type: ${{ steps.semver.outputs.release-type }}"
      #     # echo "changes: \"${{ steps.semver.outputs.changes }}\""
      #     # echo "changelog-record: \"${{ steps.semver.outputs.changelog-record }}\""
      #     # echo "scopes: \"${{ steps.semver.outputs.scopes }}\""

# refresh 2023-11-25